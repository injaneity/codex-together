(A) Add `TogetherMascotUiConfig` under `Tui` in `codex-rs/core/src/config/types.rs` with `enabled: bool` default `true`. +config +core +together +mascot
(A) Add `TogetherMascotUiConfig.max_visible: u8` default `5`. +config +core +together +mascot
(A) Add `TogetherMascotUiConfig.show_overflow_counter: bool` default `true` for `+N more`. +config +core +together +mascot
(A) Add `TogetherMascotUiConfig.show_email_labels: bool` default `true`. +config +core +together +mascot
(A) Add `TogetherMascotUiConfig.mask_email_labels: bool` default `true` (masked by default). +config +core +together +mascot
(A) Add `TogetherMascotUiConfig.landing_animation: bool` default `true`. +config +core +together +mascot
(A) Add `TogetherMascotUiConfig.landing_frame_ms: u16` default `90`. +config +core +together +mascot
(A) Add `TogetherMascotUiConfig.reduced_motion: bool` default `false` to disable non-essential mascot animation. +config +core +together +mascot +a11y
(A) Add `TogetherMascotUiConfig.render_mode: Option<String>` supporting `unicode` and `ascii`. +config +core +together +mascot
(A) Thread `tui.together_mascot_ui` through config parsing in `codex-rs/core/src/config/mod.rs`. +config +core +together +mascot
(A) Thread resolved `together_mascot_ui` into runtime `Config` in `codex-rs/core/src/config/mod.rs`. +config +core +together +mascot
(A) Add config tests for defaults and invalid `render_mode` rejection. +config +core +tests
(A) Run `just write-config-schema` after config changes. +config +schema

(A) Add `together_mascot` module export in `codex-rs/tui/src/lib.rs`. +tui +together +mascot
(A) Create `codex-rs/tui/src/together_mascot/mod.rs` exporting sprite, roster, and animation helpers. +tui +together +mascot
(A) Create `codex-rs/tui/src/together_mascot/sprite.rs` with 5 canonical generic crew sprite variants. +tui +together +mascot +assets
(A) Implement sprite rendering with TUI text + Unicode blocks only (no image assets). +tui +together +mascot +assets
(A) Add ASCII fallback sprite set for non-Unicode terminals. +tui +together +mascot +compat
(A) Add helper for deterministic slot style assignment by sorted member order. +tui +together +mascot +layout
(A) Add helper to derive compact email labels (e.g. local-part trim + width clamp). +tui +together +mascot +labels
(A) Add helper to mask email labels by default while preserving differentiability. +tui +together +mascot +labels
(A) Add label collision rules: detect duplicate rendered labels in one roster and resolve with deterministic numeric suffixes. +tui +together +mascot +labels
(A) Add secondary collision fallback: append short stable hash fragment when suffixing would overflow width. +tui +together +mascot +labels
(A) Add label width clamp rules that preserve collision suffix/hash readability under narrow width. +tui +together +mascot +labels

(A) Create `codex-rs/tui/src/together_mascot/animation.rs` with landing animation frame model. +tui +together +mascot +animation
(A) Define landing sequence frames from off-screen entry to grounded idle stance. +tui +together +mascot +animation
(A) Define leave sequence as a deterministic step-out animation for departing members. +tui +together +mascot +animation
(A) Add per-slot animation state machine (`Idle`, `Landing`). +tui +together +mascot +animation
(A) Extend per-slot animation state machine with `SteppingOut` for member leave behavior. +tui +together +mascot +animation
(A) Add frame-tick advancement by elapsed time using `landing_frame_ms`. +tui +together +mascot +animation
(A) Ensure new member arrivals trigger landing animation only once per join event. +tui +together +mascot +animation
(A) Ensure member departures trigger step-out only once per leave/removal event. +tui +together +mascot +animation
(A) Keep existing members stable (no re-landing) when roster order changes. +tui +together +mascot +animation
(A) Honor `reduced_motion` by skipping landing/step-out animation and rendering direct state transitions. +tui +together +mascot +animation +a11y

(A) Add `TogetherPresenceSnapshot` model in TUI runtime for connected members + role metadata. +tui +together +state
(A) Add mapping from together server `connected_members` to mascot roster rows. +tui +together +state
(A) Cap visible mascots at `5`; compute overflow count as `+N more`. +tui +together +state
(A) Keep deterministic display ordering to minimize visual churn across refreshes. +tui +together +state
(A) Differentiate users by email label while keeping mascot body generic. +tui +together +state

(A) Add `AppEvent::TogetherPresenceUpdated` carrying latest connected members snapshot. +tui +together +events
(A) Add lightweight together presence refresh call (`together/server/info`) after `/host`, `/join`, `/status`, `/leave`, `/close`. +tui +together +events
(A) Create background Together presence monitor task with authenticated websocket client. +tui +together +events
(A) Handle notifications `together/memberUpdated`, `together/serverClosed`, `together/connectionRevoked` in the monitor. +tui +together +events
(A) Trigger targeted roster refresh on notification receipt to update mascot strip. +tui +together +events
(A) Add polling fallback presence refresh when websocket notifications are unavailable or stale. +tui +together +events +resilience
(A) Add reconnect loop/backoff for Together presence monitor after transient transport failures. +tui +together +events +resilience
(A) Emit explicit presence connection state (`Connected`, `Reconnecting`, `Disconnected`, `Stale`) for UI rendering. +tui +together +events +state
(A) Add graceful monitor shutdown when disconnected or app exits. +tui +together +events

(A) Replace `/together` info-only output with a dedicated "Together Center" bottom-pane view. +tui +together +ui
(A) Render mascot roster as the primary visual in Together Center. +tui +together +ui
(A) Show one mascot when one member is connected. +tui +together +ui
(A) Show landing animation when additional members join and appear in roster. +tui +together +ui
(A) Show exactly five mascots max and append `+N more` overflow indicator when needed. +tui +together +ui
(A) Show short email labels under or beside each visible mascot when enabled. +tui +together +ui
(A) Show owner/member role badges using nerd-font/unicode/ascii/TUI characters only (no emoji). +tui +together +ui
(A) Show masked email labels by default in Together Center; allow unmasked only when explicitly configured. +tui +together +ui
(A) Render reconnect/stale/disconnected states in Together Center and adjust crew sprite posture/appearance accordingly. +tui +together +ui +state
(A) Add "view all members" action when roster shows `5 + N more`. +tui +together +ui
(A) Keep existing Together action hints (`/share`, `/threads`, `/history`, `/fork`, `/exit`) inside Together Center. +tui +together +ui

(A) Add optional compact mascot summary line in shared thread views (`/threads` and `/history`) if space permits. +tui +together +ui
(A) Preserve existing `/threads` and `/history` keyboard actions (`Enter`, `f`, `d`, `Esc`). +tui +together +compat
(A) Do not block command execution while mascot animations are running. +tui +together +perf
(A) Add narrow-width fallback: show email-count text when mascot strip cannot fit. +tui +together +compat

(A) Add tests for roster cap logic (`0..10` members => visible + overflow correctness). +tui +together +tests
(A) Add tests for deterministic member ordering and stable slot assignment. +tui +together +tests
(A) Add tests for email label compaction and truncation behavior. +tui +together +tests
(A) Add tests for landing animation frame progression and completion. +tui +together +tests
(A) Add tests that existing members do not re-animate on unrelated roster updates. +tui +together +tests
(A) Add snapshot test for Together Center with 1 member (single mascot). +tui +together +tests +snapshots
(A) Add snapshot test for Together Center with 2 members (second member landing sequence). +tui +together +tests +snapshots
(A) Add snapshot test for Together Center with 5 members (full visible crew). +tui +together +tests +snapshots
(A) Add snapshot test for Together Center with 7 members (5 + 2 more indicator). +tui +together +tests +snapshots
(A) Add snapshot test for ASCII fallback rendering mode. +tui +together +tests +snapshots
(A) Add integration test validating notification-driven roster refresh updates mascot view. +tui +together +tests
(A) Add integration test validating `/exit` disconnect flow clears mascot roster before app exit. +tui +together +tests
(A) Add integration test validating websocket outage falls back to polling and recovers roster updates. +tui +together +tests +resilience
(A) Add integration test validating reconnect/stale state visuals and transitions. +tui +together +tests +state
(A) Add snapshot test for masked label default and unmasked override behavior. +tui +together +tests +snapshots
(A) Add unit tests for label collision resolution (suffix + hash fallback). +tui +together +tests
(A) Add unit tests for leave step-out animation triggering and completion. +tui +together +tests
(A) Add snapshot tests for owner/member badge rendering with no emoji glyphs. +tui +together +tests +snapshots

(A) Add join/leave toast lines synchronized with mascot landing/step-out events. +tui +together +ui +notifications
(A) Add slash controls: `/together mascot on|off`, `/together mascot motion on|off`, `/together mascot labels masked|full`. +tui +together +slash
(A) Persist slash control updates through existing config edit plumbing where applicable. +tui +together +slash +config
(A) Add slash command tests for mascot toggles and label mode controls. +tui +together +slash +tests

(A) Update user-facing Together docs in `docs/` to describe Together Center mascot behavior. +docs +together
(A) Document roster cap behavior (`max 5`, overflow as `+N`). +docs +together
(A) Document that visual differentiation is by email labels, not unique character bodies. +docs +together
(A) Document that mascot rendering is terminal pixel-art via text/Unicode blocks only. +docs +together
(A) Document masked-by-default label behavior and how to opt into full labels. +docs +together
(A) Document reconnect/stale state visuals and polling fallback behavior. +docs +together
(A) Document supported mascot slash controls. +docs +together

(A) Run `just fmt` in `codex-rs` after Rust changes. +rust +fmt
(A) Run `cargo test -p codex-tui` for TUI changes and snapshot updates. +rust +tests
(A) Run `cargo insta pending-snapshots -p codex-tui` and review all mascot diffs. +rust +tests
(A) Accept intended snapshots with `cargo insta accept -p codex-tui`. +rust +tests
